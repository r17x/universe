Read and follow the Nix gateway routing protocol defined in `.claude/skills/gateway-nix.md`.

**Route the current task through the Nix domain gateway:**

1. Detect which area of Nix infrastructure is involved
2. Load relevant Tier 2 skills from `.claude/skill-library/`
3. Delegate to `nix-coder` agent with full context
4. Verify results with fast nix eval commands
5. Report back to the orchestration protocol

Start routing now.
